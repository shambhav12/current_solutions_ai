import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shambhav.currentsolutions',
  appName: 'current-solutions-ai',
  webDir: 'dist'
};

export default config;
